# @author: Deepak Singh

# Base Image
FROM dinux:0

# COPY conda-env.yml .

RUN bash && \
    # Anaconda Env
    # conda env create --name b --file conda-env.yml
    # pip install pandas scikit-learn matplotlib openpysql
    mkdir /root/py-envs && python -m venv b && \
    /root/py-envs/e1/bin/pip install 



